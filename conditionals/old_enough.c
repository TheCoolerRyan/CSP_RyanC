//Ryan Crop, old enough c
#include <stdio.h>
#include <string.h>
age;
int main(void){
    printf("What is your age?");
    scanf("%d", age);
if (age >= 18 ){
    printf("Wow because your %d you can vote!\n", age);
}
    return 0;
}